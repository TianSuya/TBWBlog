# 服务器操作指南

这个包是**完整源码**。流程：

```
本地 zip ──scp──→ 云服务器 /opt/blog-src
                       │ git push
                       ↓
                  GitHub 仓库
                       │ push 触发 Actions
                       ↓
              Actions: pnpm build + pagefind
                       │ rsync over SSH
                       ↓
              同一台服务器 /var/www/blog
                       ↑
        Caddy ─────────┘  →  https://tianbowen.net
```

源码绕一圈经过 GitHub 再回到这台服务器，是因为**服务器上不装 Node**——
构建交给 GitHub Actions，服务器只负责存源码、收成品、对外服务。

---

## 需要两套互不相同的凭据

这是整个流程里最容易搞混的地方，先说清楚：

| 凭据 | 方向 | 装在哪 | 干什么 |
|---|---|---|---|
| **A. GitHub 推送凭据** | 服务器 → GitHub | 服务器 `~/.ssh/github_push` | `git push` 源码 |
| **B. Actions 部署密钥** | GitHub → 服务器 | 私钥进 GitHub Secrets<br>公钥进服务器 `authorized_keys` | rsync 成品文件 |

两把钥匙，两个方向，别混用。下面第 2 步配 A，第 4 步配 B。

---

## 0. 上传并解压

**本地**：

```bash
export SERVER=1.2.3.4
scp tbwblog-src.zip root@$SERVER:/tmp/
```

**服务器**（root）。假设你已经做完 `deploy/README.md` 的第 2–3 步
（装好 ufw/git、建好 deploy 用户和目录）：

```bash
apt install -y unzip git
install -d -o deploy -g deploy /opt/blog-src
cd /opt/blog-src
unzip /tmp/tbwblog-src.zip
chown -R deploy:deploy /opt/blog-src
ls -A                    # 应看到 src/ public/ deploy/ .github/ package.json 等
```

> 源码放 `/opt/blog-src`，和成品目录 `/var/www/blog` 分开。
> Actions 用的是 `rsync --delete`，两者混在一起会互相删。

之后都以 deploy 用户操作：

```bash
su - deploy
cd /opt/blog-src
```

---

## 1. 检查必须改的东西

```bash
grep -n "repo:" public/admin/config.yml
grep -rn 'CHANGEME\|example\.com' src/data/*.json
```

**`repo` 必须是你真实的 GitHub 仓库**，否则浏览器后台 `/admin/` 点 Publish 会 404：

```bash
sed -i "s|repo: .*|repo: 你的用户名/你的仓库名|" public/admin/config.yml
```

`src/data/*.json` 里的 `you@example.com`、`github.com/CHANGEME` 是示例数据，
会原样显示在站点上（邮箱出现在每一页侧栏）。现在改或以后改都行，
以后改就是编辑文件再 `git push`，30–60 秒后线上更新。

域名 `tianbowen.net` 已经写进 `astro.config.mjs` 和 `deploy/Caddyfile`，不用动。

---

## 2. 配置 GitHub 推送凭据（凭据 A）

### 2.1 告诉 git 你是谁

没有这个，`git commit` 会直接报错：

```bash
git config --global user.name  "TianSuya"
git config --global user.email "actions@users.noreply.github.com"
```

### 2.2 生成 SSH 密钥

```bash
ssh-keygen -t ed25519 -f ~/.ssh/github_push -N "" -C "blog-server-push"
cat ~/.ssh/github_push.pub
```

### 2.3 加到 GitHub

复制上面输出的公钥，到仓库 → **Settings → Deploy keys → Add deploy key**：

- Title：`server-push`
- Key：粘贴公钥
- **勾选 `Allow write access`** ← 不勾就只能拉不能推

> 用仓库级 Deploy key 而不是账号级 SSH key：这把钥匙放在服务器上，
> 万一泄露，影响范围只限这一个仓库，动不了你 GitHub 账号下的其他东西。

### 2.4 让 git 用这把钥匙

```bash
cat >> ~/.ssh/config <<'EOF'
Host github.com
  IdentityFile ~/.ssh/github_push
  IdentitiesOnly yes
EOF
chmod 600 ~/.ssh/config

ssh -T git@github.com          # 首次会问 yes/no，输 yes
```

看到 `Hi <用户名>/<仓库名>! You've successfully authenticated, but GitHub does
not provide shell access.` 就对了——**这条提示是成功，不是报错**。

---

## 3. 创建 GitHub 仓库并推送

先在 github.com 新建仓库，名字和第 1 步填的 `repo` 一致。
**不要勾初始化 README / .gitignore / license**，否则 push 会冲突。

然后在服务器上：

```bash
cd /opt/blog-src
git init
git add -A
git status --short | head -20        # 扫一眼，确认没有意外文件
git commit -m "Initial commit: academic homepage and blog"
git branch -M main
git remote add origin git@github.com:你的用户名/你的仓库名.git
git push -u origin main
```

推完先别急着看 Actions——它现在会**跑到 rsync 那步失败**，因为部署密钥还没配。
这是预期的，下一步就解决。

> 检查一下 `git status --short` 有没有 `assets/originals/me.jpg`。
> 它是 3.7MB 的头像原图，已经在 `.gitignore` 里排除了；
> 如果出现在列表里说明忽略没生效，先别提交。

---

## 4. 配置 Actions 部署密钥（凭据 B）

现在要让 GitHub Actions 有权限把构建好的文件写回这台服务器。

### 4.1 生成第二对密钥

在服务器上（这次生成的**私钥要交给 GitHub**）：

```bash
ssh-keygen -t ed25519 -f ~/.ssh/blog_deploy -N "" -C "github-actions-deploy"

# 公钥装进本机，让 Actions 能 ssh 进来
cat ~/.ssh/blog_deploy.pub >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

### 4.2 打印三个值

```bash
echo "===== DEPLOY_SSH_KEY ====="
cat ~/.ssh/blog_deploy
echo "===== DEPLOY_KNOWN_HOSTS ====="
ssh-keyscan -t ed25519 $(curl -s ifconfig.me)
echo "===== DEPLOY_TARGET ====="
echo "deploy@$(curl -s ifconfig.me):/var/www/blog/"
```

### 4.3 填进 GitHub

仓库 → **Settings → Secrets and variables → Actions → Secrets** →
`New repository secret`，建三条：

| Name | 值 |
|---|---|
| `DEPLOY_SSH_KEY` | 上面第一段，**含首尾 `-----BEGIN/END-----` 行** |
| `DEPLOY_KNOWN_HOSTS` | 第二段整行 |
| `DEPLOY_TARGET` | 第三段，**结尾斜杠不能少** |

> `DEPLOY_TARGET` 漏了结尾斜杠，rsync 会把 `dist` 目录本身复制进去，
> 变成 `/var/www/blog/dist/`——**全站 404 而 Actions 显示绿灯**，最难查的一种。

### 4.4 重跑

Actions → 最近那次失败的运行 → **Re-run all jobs**。约 1–2 分钟。

绿灯后确认文件落地：

```bash
ls /var/www/blog                     # index.html blog/ zh/ assets/ pagefind/ admin/
ls /var/www/blog/pagefind | head -3
```

---

## 5. 接着装 Caddy

源码里的 `deploy/README.md` 有完整步骤，从 **第 8 步「Caddy：站点上线」** 开始看。
唯一区别：文件已经在服务器上了，那步的 `scp` 可以跳过，直接：

```bash
sudo cp /opt/blog-src/deploy/Caddyfile /etc/caddy/Caddyfile
sudo chmod +x /opt/blog-src/deploy/update-counter.sh
```

装完 Caddy 并 reload，https://tianbowen.net 就上线了。

> 注意 `deploy/README.md` 里凡是提到 `/opt/blog` 的路径，
> 在你这个方案里都是 `/opt/blog-src/deploy`。

---

## 之后怎么改内容

在服务器上编辑，然后推：

```bash
cd /opt/blog-src
vim src/content/blog/xxx.md
git add -A && git commit -m "..." && git push
```

push 完 Actions 自动构建并部署，30–60 秒后线上可见。

也可以在浏览器里写——配好 `/admin/` 后台（见 `deploy/README.md` 第 12 步），
它直接往 GitHub 提交，同样触发部署，完全不用碰服务器。

**两条路都要注意**：服务器上的 `/opt/blog-src` 和 GitHub 是两份，
在后台发过文章之后，服务器上要先 `git pull` 再改，否则下次 push 会冲突。

---

## 排错

**`git push` 报 `Permission denied (publickey)`**
Deploy key 没勾 `Allow write access`，或 `~/.ssh/config` 没写对。
先跑 `ssh -T git@github.com` 看认证是否通过。

**`git commit` 报 `Please tell me who you are`**
第 2.1 步的 `git config --global` 没做。

**push 报 `failed to push some refs`**
建仓库时勾了初始化 README。`git pull --rebase origin main` 后重试。

**Actions 里 rsync 报 `Permission denied (publickey)`**
`DEPLOY_SSH_KEY` 少了首尾的 `-----BEGIN/END-----` 行，
或第 4.1 步的公钥没写进 `~/.ssh/authorized_keys`。

**Actions 里 `Host key verification failed`**
`DEPLOY_KNOWN_HOSTS` 没填或填错，重新 `ssh-keyscan` 整行粘贴。

**Actions 在 build 步骤就失败**
看日志。多半是 `pnpm verify` 里的检查没过——本地能构建通过的话，
八成是 `src/data/*.json` 编辑时把 JSON 语法弄坏了。

**全站 404 但 Actions 绿灯**
`DEPLOY_TARGET` 结尾漏斜杠，`ls /var/www/blog` 一看便知。
